const registerSchema = require('./registerSchema');
const loginSchema = require('./loginSchema');
const subscriptionSchema = require('./subscriptiomSchema');

module.exports = { registerSchema, loginSchema, subscriptionSchema };
